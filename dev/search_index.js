var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/cumulative_cuts/#Cumulative-cuts","page":"Cumulative cuts","text":"This example shows the cumulative variant of CutHist: the cuts are treated as an ordered chain, and each per-cut histogram is filled only when all preceding cuts also pass. This makes it easy to visualise the incremental effect of stacking selections, without having to combine the predicates by hand.\n\nWe reuse the same kind of synthetic event table as in the Independent cuts example.\n\nusing Random, CSV, DataFrames\n\nrng = MersenneTwister(42)\nN = 20_000\n\ndf = DataFrame(\n    zenith        = acos.(2 .* rand(rng, N) .- 1),\n    likelihood    = 30 .+ 60 .* rand(rng, N),\n    angular_error = 0.05 .+ 2.0 .* rand(rng, N) .^ 2,\n    trigger_pct   = clamp.(0.2 .+ 0.6 .* randn(rng, N), 0, 1),\n)\n\ncsvfile = tempname() * \".csv\"\nCSV.write(csvfile, df)\nevents = CSV.read(csvfile, DataFrame)\nnothing # hide\n\nThe cuts list is the same as before, but we pass cumulative = true. With this flag, chist.hcuts[2] collects events that pass cuts 1 and 2, chist.hcuts[3] collects events that pass cuts 1, 2, and 3, and so on. The labels are prefixed with + to reflect the chain.\n\nusing Cutaway, StatsBase\n\ncuts = [\n    Cut(r -> r.likelihood    > 50;    label = \"likelihood > 50\"),\n    Cut(r -> r.angular_error < 0.5;   label = \"+ angular error < 0.5 deg\"),\n    Cut(r -> r.trigger_pct   > 0.5;   label = \"+ trigger fraction > 0.5\"),\n]\n\nbaseline = Histogram(range(-1, 1; length = 41))\nchist = CutHist(baseline, cuts; cumulative = true)\n\nfor row in eachrow(events)\n    push!(chist, cos(row.zenith), row)\nend\nchist\n\nusing CairoMakie\n\nfunction step_hist!(ax, h::Histogram; kwargs...)\n    edges = collect(h.edges[1])\n    weights = Float64.(h.weights)\n    stairs!(ax, edges, vcat(weights, last(weights)); step = :post, kwargs...)\nend\n\nfig = Figure(size = (720, 420))\nax = Axis(fig[1, 1]; xlabel = \"cos(zenith)\", ylabel = \"events / bin\")\nstep_hist!(ax, chist.h; label = \"all events\", color = :black, linewidth = 2)\nfor (cut, h) in zip(chist.cuts, chist.hcuts)\n    step_hist!(ax, h; label = cut.label)\nend\naxislegend(ax; position = :lt)\nfig\n\nEach successive line drops below the previous one because the cuts are combined automatically: every event that contributes to the third histogram has, by construction, already passed the first two.","title":"Cumulative cuts"},{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"type","location":"api/#Cutaway.AbstractCut","page":"API","text":"AbstractCut\n\nSupertype for selection criteria. Concrete subtypes must be callable, taking a single context (e.g. an event, a table row, or any user-defined record) and returning a Bool that indicates whether the context passes the cut.\n\nThe bundled Cut type wraps an arbitrary predicate function and is the recommended choice for ad-hoc cuts. To define a parameterised family of cuts, subtype AbstractCut and add call methods that dispatch on the context type.\n\n\n\n\n\n","title":"Cutaway.AbstractCut"},{"category":"type","location":"api/#Cutaway.Cut","page":"API","text":"Cut(f; label=\"\", description=\"\")\nCut(f, label, description=\"\")\n\nA cut backed by an arbitrary predicate f(context) -> Bool, optionally annotated with a short label (used in plots and printouts) and a longer description.\n\nBoth keyword and positional forms are accepted:\n\nCut(r -> r.likelihood > 50; label=\"likelihood > 50\")\nCut(r -> r.likelihood > 50, \"likelihood > 50\")\n\n\n\n\n\n","title":"Cutaway.Cut"},{"category":"type","location":"api/#Cutaway.CutHist","page":"API","text":"CutHist(h::AbstractHistogram, cuts; cumulative=false)\n\nBundle a base histogram h with a list of cuts and one per-cut histogram allocated as a deepcopy of h. A subsequent push!(chist, x, context) always fills the base histogram with x, and additionally fills each per-cut histogram according to whether the cuts pass when applied to context.\n\nWhen cumulative=false (the default), each cut is evaluated independently: the per-cut histogram hcuts[i] is filled whenever cuts[i](context) returns true.\n\nWhen cumulative=true, the cuts are treated as an ordered chain. The loop short-circuits on the first failing cut, so hcuts[i] only fills when all of cuts[1], cuts[2], ..., cuts[i] pass. This is convenient for visualising the cumulative effect of successive selections without having to combine the predicates by hand.\n\nThe split between x (the value being histogrammed) and context (the record the cuts inspect) lets cuts examine a richer object than the scalar that ends up in the histogram.\n\n\n\n\n\n","title":"Cutaway.CutHist"},{"category":"function","location":"api/#Cutaway.iscumulative","page":"API","text":"iscumulative(c::CutHist)\n\nReturn true if c applies its cuts as an ordered chain (each per-cut histogram is filled only when all preceding cuts also pass), and false if it evaluates each cut independently.\n\n\n\n\n\n","title":"Cutaway.iscumulative"},{"category":"method","location":"api/#Base.push!-Tuple{CutHist, Any, Any}","page":"API","text":"push!(chist::CutHist, x, context)\n\nAdd the value x to the base histogram of chist, and to each per-cut histogram whose cut passes when applied to context. See CutHist for the difference between the cumulative and independent semantics.\n\n\n\n\n\n","title":"Base.push!"},{"category":"section","location":"manual/#Manual","page":"Manual","text":"","title":"Manual"},{"category":"section","location":"manual/#Defining-cuts","page":"Manual","text":"A cut in Cutaway.jl is a callable that takes a single context (an event, a table row, or any user-defined record) and returns a Bool indicating whether the context passes the cut. The bundled Cut type covers the common case: it wraps an arbitrary predicate function and stores a short label and an optional longer description.\n\nusing Cutaway\n\nc = Cut(r -> r.likelihood > 50; label = \"likelihood > 50\")\nc((likelihood = 73,))\n\nThe positional form Cut(predicate, label[, description]) is also accepted, which is convenient when defining a vector of cuts inline:\n\ncuts = [\n    Cut(r -> r.likelihood    > 50,   \"likelihood > 50\"),\n    Cut(r -> r.angular_error < 0.5,  \"angular error < 0.5 deg\"),\n]\n\nCuts are then bundled with a base histogram into a CutHist. On every push!(chist, x, context), the value x is added to the base histogram and to each per-cut histogram whose cut passes when applied to context. The split between x (what gets histogrammed) and context (what the cuts inspect) lets cuts examine a richer record than the scalar that ends up in the histogram - typically x is a single derived quantity and context is the full row or event.\n\nusing StatsBase\n\nchist = CutHist(Histogram(range(0, 1; length = 21)), cuts)\npush!(chist, 0.7, (likelihood = 73, angular_error = 0.2))\nchist\n\nSee the Independent cuts and Cumulative cuts examples for the difference between the two evaluation modes.","title":"Defining cuts"},{"category":"section","location":"manual/#Advanced-cuts:-parameterised-cuts","page":"Manual","text":"The plain Cut wrapper is fine for one-off predicates, but it gets awkward when:\n\nthe same logic is reused with different parameters (e.g. several likelihood thresholds in the same analysis),\nthe cut needs to act on different context types (e.g. a generic table row in some pipelines and a typed event struct in others),\nthe parameters and metadata are interesting to inspect or print.\n\nIn those cases, define your own subtype of AbstractCut. The only requirement is that instances are callable and return a Bool. Multiple call methods can dispatch on the context type so that the same cut works across data representations.\n\nusing Cutaway\n\n@enum CutOp GT GE LT LE\n\nfunction _apply(op::CutOp, val, threshold)\n    op === GT ? val >  threshold :\n    op === GE ? val >= threshold :\n    op === LT ? val <  threshold :\n                val <= threshold\nend\n\nstruct LikelihoodCut <: AbstractCut\n    threshold::Float64\n    op::CutOp\n    label::String\n    description::String\nend\n\nLikelihoodCut(threshold, op; label = \"\", description = \"\") =\n    LikelihoodCut(threshold, op, label, description)\nnothing # hide\n\nA single shared helper applies the cut, and one short call method per context type plugs it into the relevant data representation. Here we show two: a generic table row that exposes a likelihood column, and a hypothetical event struct that stores it under a different name.\n\nstruct Event\n    lik::Float64\n    zenith::Float64\nend\n\n(c::LikelihoodCut)(row)           = _apply(c.op, row.likelihood, c.threshold)\n(c::LikelihoodCut)(event::Event)  = _apply(c.op, event.lik,      c.threshold)\nnothing # hide\n\nThe cut now works on both representations without the call site having to know which one is in use:\n\ncut = LikelihoodCut(50.0, GT; label = \"likelihood > 50\")\n\ncut((likelihood = 73.0,))      # generic row\n\ncut(Event(73.0, 0.4))          # typed event\n\nAnd, because LikelihoodCut <: AbstractCut, instances drop into a CutHist exactly like a Cut does:\n\nusing StatsBase\n\nchist = CutHist(\n    Histogram(range(-1, 1; length = 21)),\n    [LikelihoodCut(50.0, GT; label = \"lik > 50\")],\n)\npush!(chist, cos(0.4), Event(73.0, 0.4))\nchist\n\nA few practical notes:\n\nKeep a label field (and ideally a description) on every AbstractCut subtype. The example pages reach into cut.label to populate plot legends, and following the same convention keeps custom cuts plot-compatible.\nThe dispatch pattern shown above scales: add another call method for every new context type you want to support (a database row, an HDF5 group, a different event type, ...). The cut object stays the same; the new method makes it understand the new representation.\nFor cuts that share a common shape (e.g. all \"compare a single field to a threshold\"), it is often worth factoring out a small helper such as _apply above so each new cut type is a thin wrapper over it.","title":"Advanced cuts: parameterised cuts"},{"category":"section","location":"#Cutaway","page":"Home","text":"(Image: Stable) (Image: Dev) (Image: Build Status)\n\nCutaway.jl bundles a histogram with a list of cuts and fills one filtered histogram per cut as you stream data through it. Cuts can be applied independently (each cut on its own) or cumulatively (each cut on top of all preceding ones).","title":"Cutaway"},{"category":"section","location":"#Installation","page":"Home","text":"Cutaway.jl is registered in the Julia General registry:\n\njulia> import Pkg; Pkg.add(\"Cutaway\")","title":"Installation"},{"category":"section","location":"#Quickstart","page":"Home","text":"using Cutaway, StatsBase\n\nrows = [(zenith = π * rand(), likelihood = 100 * rand()) for _ in 1:10_000]\n\ncuts = [\n    Cut(r -> r.likelihood > 50;            label = \"likelihood > 50\"),\n    Cut(r -> cos(r.zenith) < 0;            label = \"upgoing\"),\n]\n\nchist = CutHist(Histogram(range(-1, 1; length = 41)), cuts)\n\nfor r in rows\n    push!(chist, cos(r.zenith), r)\nend\n\nchist.h            # baseline histogram, all events\nchist.hcuts[1]     # events that passed `likelihood > 50`\nchist.hcuts[2]     # events that passed `upgoing`\n\nPass cumulative = true to the CutHist constructor to apply the cuts as an ordered chain instead, so chist.hcuts[i] only fills when cuts 1..i all pass.\n\nPlotting examples with CairoMakie are available under Independent cuts and Cumulative cuts.","title":"Quickstart"},{"category":"section","location":"examples/fhist/#Using-FHist.jl-as-the-histogram-backend","page":"Using FHist.jl as the histogram backend","text":"FHist.jl provides a richer histogram type (Hist1D, Hist2D, ...) than StatsBase.Histogram, with first-class support for weighted entries, pulls, and Makie plotting. Its HistXD <: StatsBase.AbstractHistogram, so it drops into CutHist without any extra wiring.\n\nThe two main ergonomic wins compared to the Independent cuts example are:\n\nHist1D(; binedges = ...) constructs an empty, zero-filled histogram in one call, with named-argument binning.\nFHist ships Makie recipes, so stairs!(ax, h) plots a Hist1D directly - no helper around stairs!(edges, weights) needed.\n\nWe reuse the same synthetic event table as in the other examples.\n\nusing Random, CSV, DataFrames\n\nrng = MersenneTwister(42)\nN = 20_000\n\ndf = DataFrame(\n    zenith        = acos.(2 .* rand(rng, N) .- 1),\n    likelihood    = 30 .+ 60 .* rand(rng, N),\n    angular_error = 0.05 .+ 2.0 .* rand(rng, N) .^ 2,\n    trigger_pct   = clamp.(0.2 .+ 0.6 .* randn(rng, N), 0, 1),\n)\nevents = df\nnothing # hide\n\nThe CutHist construction is the same as before; only the histogram type changes.\n\nusing Cutaway, FHist\n\ncuts = [\n    Cut(r -> r.likelihood    > 50;    label = \"likelihood > 50\"),\n    Cut(r -> r.angular_error < 0.5;   label = \"angular error < 0.5 deg\"),\n    Cut(r -> r.trigger_pct   > 0.5;   label = \"trigger fraction > 0.5\"),\n]\n\nbaseline = Hist1D(; binedges = range(-1, 1; length = 41))\nchist = CutHist(baseline, cuts)\n\nfor row in eachrow(events)\n    push!(chist, cos(row.zenith), row)\nend\nchist\n\nFHist exposes the underlying counts and edges through accessor functions, which is handy for quick numerical checks alongside the plot.\n\n(total = integral(chist.h),\n after_likelihood    = integral(chist.hcuts[1]),\n after_angular_error = integral(chist.hcuts[2]),\n after_trigger       = integral(chist.hcuts[3]))\n\nPlotting is a direct call to Makie's stairs!, with no helper:\n\nusing CairoMakie\n\nfig = Figure(size = (720, 420))\nax = Axis(fig[1, 1]; xlabel = \"cos(zenith)\", ylabel = \"events / bin\")\nstairs!(ax, chist.h; label = \"all events\", color = :black, linewidth = 2)\nfor (cut, h) in zip(chist.cuts, chist.hcuts)\n    stairs!(ax, h; label = cut.label)\nend\naxislegend(ax; position = :lt)\nfig\n\nThe same code works unchanged with cumulative = true to chain the cuts (see Cumulative cuts).","title":"Using FHist.jl as the histogram backend"},{"category":"section","location":"examples/independent_cuts/#Independent-cuts","page":"Independent cuts","text":"This example shows how CutHist can be used to evaluate several cuts independently against the same dataset. Each cut produces its own filtered histogram, side by side with the unfiltered baseline.\n\nWe start by generating a small CSV file that mimics a reconstruction output, with one row per event and a handful of columns: the zenith angle and three quality estimators.\n\nusing Random, CSV, DataFrames\n\nrng = MersenneTwister(42)\nN = 20_000\n\ndf = DataFrame(\n    zenith        = acos.(2 .* rand(rng, N) .- 1),       # uniform in cos(zenith)\n    likelihood    = 30 .+ 60 .* rand(rng, N),            # uniform in 30..90\n    angular_error = 0.05 .+ 2.0 .* rand(rng, N) .^ 2,    # peaks near 0.05 deg\n    trigger_pct   = clamp.(0.2 .+ 0.6 .* randn(rng, N), 0, 1),\n)\n\ncsvfile = tempname() * \".csv\"\nCSV.write(csvfile, df)\nnothing # hide\n\nWe read the CSV back and define three cuts. Each Cut wraps an ad-hoc predicate that inspects a single row and returns a Bool. The label is reused below in the plot legend.\n\nusing Cutaway, StatsBase\n\nevents = CSV.read(csvfile, DataFrame)\n\ncuts = [\n    Cut(r -> r.likelihood    > 50;    label = \"likelihood > 50\"),\n    Cut(r -> r.angular_error < 0.5;   label = \"angular error < 0.5 deg\"),\n    Cut(r -> r.trigger_pct   > 0.5;   label = \"trigger fraction > 0.5\"),\n]\n\nbaseline = Histogram(range(-1, 1; length = 41))\nchist = CutHist(baseline, cuts)\n\nfor row in eachrow(events)\n    push!(chist, cos(row.zenith), row)\nend\nchist\n\nEach cut is evaluated independently against the row, so chist.hcuts[i] reflects the effect of cut i alone, regardless of the other cuts.\n\nTo plot a pre-binned Histogram with Makie we use stairs! directly. The small helper below is reused in the next example.\n\nusing CairoMakie\n\nfunction step_hist!(ax, h::Histogram; kwargs...)\n    edges = collect(h.edges[1])\n    weights = Float64.(h.weights)\n    stairs!(ax, edges, vcat(weights, last(weights)); step = :post, kwargs...)\nend\n\nfig = Figure(size = (720, 420))\nax = Axis(fig[1, 1]; xlabel = \"cos(zenith)\", ylabel = \"events / bin\")\nstep_hist!(ax, chist.h; label = \"all events\", color = :black, linewidth = 2)\nfor (cut, h) in zip(chist.cuts, chist.hcuts)\n    step_hist!(ax, h; label = cut.label)\nend\naxislegend(ax; position = :lt)\nfig","title":"Independent cuts"}]
}
